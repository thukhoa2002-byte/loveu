index.html
<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>HAPPY VIETNAMESE WOMEN'S DAY — Bloom Effect</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    html,body { height: 100%; }

    :root {
      --pink1: #ff7eb9;
      --pink2: #ffbfd3;
      --white: #fff;
      --gold: #ffd9a0;
      --bg1: #fff5f9;
      --bg2: #ffeef6;
      --shadow: rgba(255, 105, 180, 0.18);
    }

    body {
      font-family: "Poppins", "Montserrat", sans-serif;
      background: radial-gradient(circle at 50% 20%, var(--bg2), var(--bg1) 70%);
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
      min-height: 100vh;
    }

    .stage {
      position: relative;
      text-align: center;
      padding: 60px 30px;
      border-radius: 24px;
      background: rgba(255,255,255,0.6);
      backdrop-filter: blur(8px);
      box-shadow: 0 10px 40px rgba(255, 105, 180, 0.1);
    }

    /* Hiệu ứng xuất hiện từng chữ */
    .headline {
      font-weight: 800;
      letter-spacing: 0.05em;
      font-size: clamp(44px, 8vw, 110px);
      line-height: 1.05;
      display: inline-block;
      overflow: hidden;
      position: relative;
      color: transparent;
    }

    .headline span {
      display: inline-block;
      opacity: 0;
      transform: scale(0.7) translateY(50px);
      background: linear-gradient(90deg, var(--pink1), var(--pink2), var(--gold));
      -webkit-background-clip: text;
      background-clip: text;
      animation: fadeUp 0.8s ease forwards;
      text-shadow: 0 3px 12px rgba(255,192,203,0.5);
    }

    .headline::after {
      content: "";
      position: absolute;
      top: 0; left: -80%;
      width: 60%;
      height: 100%;
      background: linear-gradient(120deg, transparent, rgba(255,255,255,0.6), transparent);
      animation: shine 3s ease-in-out 1s infinite;
    }

    @keyframes fadeUp {
      0% { opacity: 0; transform: scale(0.7) translateY(50px); }
      60% { opacity: 1; transform: scale(1.05) translateY(-10px); }
      100% { opacity: 1; transform: scale(1) translateY(0); }
    }

    @keyframes shine {
      0% { left: -80%; }
      50% { left: 120%; }
      100% { left: 120%; }
    }

    .sub {
      font-size: clamp(14px, 2vw, 22px);
      color: #b06b8e;
      margin-top: 12px;
      font-weight: 600;
    }

    /* Hiệu ứng hoa nở */
    .flower {
      position: absolute;
      width: 30px; height: 30px;
      opacity: 0;
      transform: scale(0);
      animation: bloom 3.5s ease-in-out infinite;
    }

    @keyframes bloom {
      0% { transform: scale(0) rotate(0deg); opacity: 0; }
      20% { transform: scale(1.3) rotate(45deg); opacity: 1; }
      80% { transform: scale(1) rotate(180deg); opacity: 0.9; }
      100% { transform: scale(0) rotate(270deg); opacity: 0; }
    }

    /* Tim bay */
    .heart {
      position: absolute;
      width: 18px;
      height: 18px;
      animation: floatUp linear infinite;
      opacity: 0;
    }

    @keyframes floatUp {
      0% { transform: translateY(0) rotate(0deg); opacity: 0; }
      10% { opacity: 1; }
      100% { transform: translateY(-80vh) rotate(80deg); opacity: 0; }
    }
  </style>
</head>
<body>
  <main class="stage">
    <h1 class="headline" id="headline"></h1>
    <p class="sub">🌸 Chúc Bé 20/10 luôn xinh, hạnh phúc và cười tươi
    mỗi ngày em nhé 🌸</p>
    <p class="sub">💗 Love u 💗</p>
    <div id="effects"></div>
  </main>

  <script>
    const lines = [
      "HAPPY VIETNAMESE",
      "WOMEN'S DAY"
    ];

    const headline = document.getElementById("headline");

    let charIndex = 0;
    lines.forEach(line => {
      const lineContainer = document.createElement('div');
      lineContainer.style.display = 'block';
      line.split("").forEach(char => {
        const span = document.createElement("span");
        span.textContent = char === " " ? "\u00A0" : char;
        span.style.setProperty("--i", charIndex++);
        lineContainer.appendChild(span);
      });
      headline.appendChild(lineContainer);
    });

    const container = document.getElementById('effects');
    const flowerColors = ['#ffb7d5','#ffd6e0','#ffc1c1','#ffe0d1','#fff1e9','#ff99c8'];

    function flowerSVG(color) {
      return `<svg viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg">
        <path fill="${color}" d="M32 10c3 0 5 3 5 6 0 2-1 4-2 5 2-1 5-2 7-2 3 0 6 2 6 5s-3 5-6 5c-2 0-5-1-7-2 1 1 2 3 2 5 0 3-2 6-5 6s-5-3-5-6c0-2 1-4 2-5-2 1-5 2-7 2-3 0-6-2-6-5s3-5 6-5c2 0 5 1 7 2-1-1-2-3-2-5 0-3 2-6 5-6z"/>
      </svg>`;
    }

    function heartSVG(color){
      return `<svg viewBox="0 0 32 29" xmlns="http://www.w3.org/2000/svg">
        <path fill="${color}" d="M23.6 2.6c-2.7 0-4.3 2.1-4.6 2.7-.3-.6-1.9-2.7-4.6-2.7-3.9 0-7.4 3.6-7.4 7.8 0 8.8 11 12.3 12 13 .9-.6 12-4.2 12-13 0-4.2-3.5-7.8-7.4-7.8z"/>
      </svg>`;
    }

    function spawnFlowers(n=10){
      for(let i=0;i<n;i++){
        const f=document.createElement('div');
        f.className='flower';
        const c=flowerColors[Math.floor(Math.random()*flowerColors.length)];
        f.innerHTML=flowerSVG(c);
        f.style.left=`${30+Math.random()*40}%`;
        f.style.top=`${30+Math.random()*40}%`;
        f.style.animationDelay=`${Math.random()*2}s`;
        f.style.transformOrigin='center';
        f.style.zIndex=0;
        container.appendChild(f);
      }
    }

    function spawnHearts(n=15){
      for(let i=0;i<n;i++){
        const h=document.createElement('div');
        h.className='heart';
        const color=['#ff9fc9','#ffc0da','#ffd9ec'][Math.floor(Math.random()*3)];
        h.innerHTML=heartSVG(color);
        h.style.left=`${Math.random()*100}%`;
        h.style.animationDuration=`${6+Math.random()*6}s`;
        h.style.animationDelay=`${Math.random()*3}s`;
        container.appendChild(h);
      }
    }

    spawnFlowers(10);
    spawnHearts(15);
  </script>
</body>
</html>
